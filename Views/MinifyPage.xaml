<Page
    x:Class="AeternamDonaEis.Views.MinifyPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AeternamDonaEis.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource Schwarz}">

    <StackPanel Padding="32 8" Orientation="Vertical">
        <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled" 
                      VerticalScrollMode="Auto" Height="240" Width="400">
            <ListBox ItemsSource="{x:Bind ViewModel.LstFiles, Mode=TwoWay}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Name}" FontSize="=32" HorizontalAlignment="Left"></TextBlock>
                            <TextBlock Text="X" FontSize="32" HorizontalAlignment="Right"></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>
        <Button Command="{x:Bind ViewModel.PickFileCommand}" Content="Choose Files!" HorizontalAlignment="Right" 
                Width="168" Margin="0 8"></Button>
        <TextBox Text="{x:Bind ViewModel.Path, Mode=TwoWay}" IsEnabled="False"></TextBox>
        <StackPanel Orientation="Vertical" Margin="0 8" HorizontalAlignment="Right">
            <Button Command="{x:Bind ViewModel.PickFolderCommand}" Content="Choose Folder" 
                    Width="168" Margin="0 0 0 8"></Button>
            <Button Command="{x:Bind ViewModel.MinifyCommand}" Content="Minify" Width="168" IsEnabled="{x:Bind ViewModel.MinifyEnabled, Mode=TwoWay}"></Button>
        </StackPanel>
    </StackPanel>
</Page>
